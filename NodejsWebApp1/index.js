/*const express = require('express');
const app = express();
const port = 3000;

app.get('/', (req, res) => {
    res.send('<h1>Node.jsテストサイト</h1><p>ボタンを押してみてください。</p><button onclick="alert(\'ボタンが押されました！\')">押す</button>');
});

app.listen(port, () => {
    console.log(`サーバーが起動しました。http://localhost:${port}`);
});
*/